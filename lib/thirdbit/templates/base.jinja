<!DOCTYPE html>
<html lang="en">
  <head>
    {% include "head.jinja" %}
    <link rel="stylesheet" href="@root/files/chota.css" type="text/css">
    <link rel="stylesheet" href="@root/files/mccole.css" type="text/css">
    <link rel="stylesheet" href="@root/files/thirdbit.css" type="text/css">
    {% block head_title %}
    <title>{% if node.title %}{{node.title}}{% else %}{{site.title}}{% endif %}</title>
    {% endblock %}
    {% block head_includes %}{% endblock %}
    {% if node.katex %}<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex/dist/katex.min.css">{% endif %}
  </head>
  <body>
    <a class="skip-link" href="#main-content">Skip to main content</a>
    {% include "nav.jinja" %}
    <main id="main-content">
      {% block page_title %}
      <h1>{% if node.title %}{{node.title}}{% else %}{{site.title}}{% endif %}</h1>
      {% endblock %}
{% block content %}{% endblock %}
    </main>
    {% include "foot.jinja" %}
    {% if node.katex %}
    <script src="https://cdn.jsdelivr.net/npm/katex/dist/katex.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/katex/dist/contrib/auto-render.min.js"></script>
    <script>
      renderMathInElement(document.body, {
	  delimiters: [
              { left: "$$", right: "$$", display: true  },
              { left: "$",  right: "$",  display: false }
	  ]
      });
    </script>
    {% endif %}
  </body>
</html>
